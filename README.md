# philbin

Small web apps + workflow tools built iteratively.

## Layout
- `apps/` — web apps (some deployed via GitHub Pages)
- `tools/` — small workflow helpers/scripts
- `docs/` — logs, how-tos, ideas

## Nightly builds
See `docs/NIGHTLY_BUILDS.md`.
